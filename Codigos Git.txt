git init  >> criar
git status >> verificar os arquivos
git add "nome do arquivo" >> adicionar 1
git add . >> adiciona todos pendentes
git commit -m "mensagem"
git push >> envia os arquivos
git remote add origin (url do repositorio) >> aponta repositorio
git reflog >> ver histórico
git reset --hard (codigo do reflog)
git branch staging (criar branch) >> copia segura
git checkout staging >> mudar a branch
git pull >> trazer do servidor para a maquina
git merge (nome da branch) >> dar merge 
git pull request
touch .gitignore >> ignora pastas e arquivos
Real
git pull
git checkout -b sistema-de-login master


